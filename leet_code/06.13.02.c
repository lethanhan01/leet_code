#include <stdio.h>
#include <math.h>
int main() {
    int n;
    int Q;
    int m=0;
    printf("HELLO\n");
    /*int a[10000]={0};
    scanf("%d %d", &n, &Q);
    for(int i=0; i<n; i++) {
        scanf("%d", &a[i]);
    }
    for(int i=0; i<n; i++) {
        for(int j=0;j<i;j++){
            if(a[j]+a[i]==Q){
                m++;
            }
        }
    }
    printf("%d", (m+1)/2);*/
    return 0;
}
